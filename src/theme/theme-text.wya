<template>
	<text 
		src="{{ source }}"
		style="{{ styles }} {{ mcStyle }}"
		class="mc-theme-text mc-class"
	>	
		<slot />
	</text>
</template>
<script>
import McComponent from '../common/component';
import theme from './behavior';

McComponent({
	mixins: [theme],
	props: {
		// TODO, text 有关的变量
	}
});

</script>

<config>
{
	"component": true
}
</config>
